class CounterTransactionEntity {
  int id;
  int totalTransaction;
  DateTime dateTime;

  CounterTransactionEntity({
    required this.id,
    required this.totalTransaction,
    required this.dateTime,
  });
}
